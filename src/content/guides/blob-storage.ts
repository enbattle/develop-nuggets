import body from './blob-storage.md?raw';
import type { Nugget } from '@/types';

export const blobStorage: Nugget = {
  id: 'blob-storage',
  title: 'Blob Storage',
  tags: ['tooling', 'apis'],
  body,
  format: 'guide',
};
