import body from './model-context-protocol.md?raw';
import type { Nugget } from '@/types';

export const modelContextProtocol: Nugget = {
  id: 'model-context-protocol',
  title: 'Model Context Protocol (MCP)',
  summary:
    'The open protocol that lets a tool built once work with any compliant model host, and how to write an MCP server.',
  tags: ['agents', 'tooling', 'apis'],
  section: 'ai-agents',
  body,
  format: 'guide',
};
