import body from './model-context-protocol.md?raw';
import type { Nugget } from '@/types';

export const modelContextProtocol: Nugget = {
  id: 'model-context-protocol',
  title: 'Model Context Protocol (MCP)',
  // TODO(phase-2): one plain-text sentence — what this is *for* / when to reach for it.
  summary: 'TODO: summary pending Phase 2 content pass.',
  tags: ['ai', 'networking', 'patterns', 'tooling'],
  section: 'ai-agents',
  body,
  format: 'guide',
};
